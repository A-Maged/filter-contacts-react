{
  "name": "react-test",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://a-maged.github.io/filter-contacts-react/",
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4",
    "react-transition-group": "^2.4.0",
    "uniqid": "^5.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
	"eject": "react-scripts eject",

	"build-deploy": "rm -rf docs/ && npm run build && mv build docs"
  }
}
